import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary & Accent
  static const primary = Color(0xFF0F172A); // Obsidian Navy
  static const primaryLight = Color(0xFF1E293B);
  static const primaryDark = Color(0xFF0B1120);
  static const accent = Color(0xFF4F46E5); // Electric Indigo
  static const accentLight = Color(0xFFEEF2FF);
  static const accentGradientStart = Color(0xFF4F46E5);
  static const accentGradientEnd = Color(0xFF7C3AED);

  // Background & Surfaces
  static const background = Color(0xFFF8FAFC); // Clean Slate
  static const surface = Color(0xFFFFFFFF);
  static const cardSurface = Color(0xFFFFFFFF);
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);

  // Text Colors
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);

  // Functional & Semantic Colors
  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFFEF2F2);
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFFECFDF5);
  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFFFBEB);
  static const info = Color(0xFF3B82F6);
  static const infoLight = Color(0xFFEFF6FF);

  // Priority Colors (Soft Neon / Vivid)
  static const priorityLow = Color(0xFF10B981);
  static const priorityMedium = Color(0xFFF59E0B);
  static const priorityHigh = Color(0xFFEF4444);
  static const priorityCritical = Color(0xFF8B5CF6);

  // Status Colors
  static const statusTodo = Color(0xFF64748B);
  static const statusInProgress = Color(0xFF3B82F6);
  static const statusDone = Color(0xFF10B981);
}
